# hivelyplugin

Amiga **AHX** and **HivelyTracker** music.

Extensions: `.ahx` `.hvl` `.thx`

`.thx` **is** `.ahx` — the same format under THX's original extension (magic
`THX\x01`), so it is claimed here rather than routed to UADE.
