# v2plugin

**Farbrausch V2** Synthesizer System modules.

Extensions: `.v2` `.v2m`

The V2 synth is rendered in-process; `.v2m` files carry the sequencer data and
patch set together, so no companion files are needed.
