# ptkplugin — ProTrekkr / NoiseTrekker

**ProTrekkr** music by Franck Charlet (Hitchhikr) — the synth+sample tracker
descended from NoiseTrekker.

Extensions: `.ptk` `.ntk`

Modules are zlib-compressed (`BZR2` container) and are inflated in memory before
the replayer runs.
